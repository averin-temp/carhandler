
git pull    // обновление через гит    
cd build    // перейти в папку build
cmake ..    // Сгенерировать проект через CMake ( ..  - путь к файлу CMakeLists)
make        // Сборка проекта



для работы нужно запустить pigpiod - это сервер управления пинами, через него работает драйвер машинки
sudo pigpiod  


sudo ./CarHandler
  ^  - это обязательно 
